@model IEnumerable<Domain.UniversitySolution.University>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UnivesityName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UniversitySlogan)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UnivesityName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UniversitySlogan)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UniversityId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UniversityId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UniversityId })
        </td>
    </tr>
}

</table>
